development:
  aws:
    access_key_id: key
    secret_access_key: secret
    bucket_name: media.something.com
  redis:
    persisted:
      host: 127.0.0.1
      db: 0
    cache:
      host: 127.0.0.1
      db: 1
test:
  aws:
    access_key_id: key
    secret_access_key: secret
    bucket_name: media.something.com
  redis:
    persisted:
      host: 127.0.0.1
      db: 0
    cache:
      host: 127.0.0.1
      db: 1
